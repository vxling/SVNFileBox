<Window x:Class="SVNFileBox.Windows.AddLocalRepoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource AddLocalRepoTitle}" Height="180" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource LocalPath}" VerticalAlignment="Center" FontSize="13"/>
        <TextBox x:Name="LocalPathBox" Grid.Row="0" Grid.Column="1" IsReadOnly="True" Padding="4,3" FontSize="13"/>
        <Button Content="{DynamicResource Browse}" Grid.Row="0" Grid.Column="2" Margin="5,0,0,0" Click="Browse_Click" MinWidth="80" Padding="12,6"/>

        <TextBlock x:Name="ErrorText" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                   Foreground="Red" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap" MaxWidth="400" HorizontalAlignment="Left"/>

        <TextBlock x:Name="StatusText" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3"
                   FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="OkButton" Content="{DynamicResource Confirm}" Margin="0,0,10,0" Click="OK_Click" MinWidth="80" Padding="12,6"/>
            <Button x:Name="CancelButton" Content="{DynamicResource Cancel}" Click="Cancel_Click" MinWidth="80" Padding="12,6"/>
        </StackPanel>
    </Grid>
</Window>
