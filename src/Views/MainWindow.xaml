<Window x:Class="SVNFileBox.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SVN FileBox" Height="768" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="HeaderButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#0078D4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5F1FB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#106EBE"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#999999"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#DC3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#6C757D"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A6268"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar -->
        <Border Grid.Column="0" Background="White" Margin="8"
                CornerRadius="8" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- App Header -->
                <StackPanel Grid.Row="0" Margin="16,16,16,8">
                    <TextBlock Text="SVN FileBox" FontSize="18" FontWeight="SemiBold" Foreground="#0078D4"/>
                    <TextBlock x:Name="VersionText" Text="v1.0.1" FontSize="11" Foreground="#999999" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Repo List -->
                <ListBox Grid.Row="1" x:Name="RepoListBox"
                         BorderThickness="0"
                         Margin="8,0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         SelectionChanged="RepoListBox_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8,6" Margin="0,1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="Medium"
                                               TextTrimming="CharacterEllipsis" Foreground="#333333"/>
                                    <TextBlock Text="{Binding LocalPath}" FontSize="10" Foreground="#999999"
                                               TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd" Background="Transparent"
                                                BorderThickness="0" CornerRadius="4">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#E5F1FB"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#CCE4F7"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="2" Margin="8,8,8,12">
                    <Button Content="添加本地仓库" Style="{StaticResource ActionButton}"
                            Click="AddLocalRepo_Click" Margin="0,0,0,6"/>
                    <Button Content="添加 SVN 仓库" Style="{StaticResource ActionButton}"
                            Click="AddSvnRepo_Click" Margin="0,0,0,6"/>
                    <Button Content="设置" Style="{StaticResource SecondaryButton}"
                            Click="Settings_Click" Margin="0,0,0,6"/>
                    <Button Content="关于" Style="{StaticResource SecondaryButton}"
                            Click="About_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Border Grid.Column="1" Background="White" Margin="0,8,8,8"
                CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Border Grid.Row="0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="16,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock x:Name="RepoNameText" Text="未选择仓库" FontSize="16" FontWeight="SemiBold"
                                       VerticalAlignment="Center" Foreground="#333333"/>
                            <TextBlock x:Name="RepoStatusText" Text="" FontSize="12" Foreground="#666666"
                                       VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="CommitBtn" Content="提交" Style="{StaticResource ActionButton}"
                                    Click="Commit_Click" IsEnabled="False" Margin="0,0,8,0"/>
                            <Button x:Name="UpdateBtn" Content="更新" Style="{StaticResource ActionButton}"
                                    Click="Update_Click" IsEnabled="False" Margin="0,0,8,0"/>
                            <Button x:Name="RefreshBtn" Content="刷新" Style="{StaticResource SecondaryButton}"
                                    Click="Refresh_Click" IsEnabled="False" Margin="0,0,8,0"/>
                            <Button x:Name="RevertBtn" Content="还原" Style="{StaticResource DangerButton}"
                                    Click="Revert_Click" IsEnabled="False"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- File List -->
                <TreeView Grid.Row="1" x:Name="FileTreeView"
                          BorderThickness="0"
                          Margin="8"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            <Setter Property="Padding" Value="4,2"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>

                <!-- Status Bar -->
                <StatusBar Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
                    <StatusBarItem>
                        <TextBlock x:Name="StatusText" Text="就绪" FontSize="12" Foreground="#666666"/>
                    </StatusBarItem>
                    <Separator/>
                    <StatusBarItem>
                        <TextBlock x:Name="ProgressText" Text="" FontSize="12" Foreground="#666666"/>
                    </StatusBarItem>
                </StatusBar>

                <!-- Loading Overlay -->
                <Border Grid.Row="0" Grid.RowSpan="3" x:Name="LoadingOverlay"
                        Background="#80FFFFFF" CornerRadius="8"
                        Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar x:Name="LoadingProgress" IsIndeterminate="True"
                                     Width="200" Height="4" Margin="0,0,0,12"/>
                        <TextBlock x:Name="LoadingText" Text="处理中..." FontSize="13"
                                   HorizontalAlignment="Center" Foreground="#666666"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
