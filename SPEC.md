# SVNFileBox - SPEC.md

> SVN 版 Dropbox。本地文件夹自动同步到 SVN 仓库，多设备共享。

---

## 核心设计决策

### 冲突策略：Last-Write-Wins

- 以**本地文件最后修改时间**为准，强制覆盖 SVN 端版本
- 无合并、无保留多版本，冲突直接解决
- **理由**: 简单粗暴，对用户友好，避免手动解决冲突的复杂度

### 同步粒度：文件级

- 检测到文件变化（Create/Modify/Delete/Rename）立即提交整个文件
- 不做分块、不做增量，文件级简单直接
- **注意**: SVN 本身版本控制是全文件快照，天然契合此策略

### 手工操作

- **暂不提供**任何手工操作入口（手动提交、手动 checkout 等）
- 仅提供**同步记录**让用户查看历史
- **理由**: MVP 阶段先跑通核心流程，手工操作留作后续迭代

---

## 功能列表

### 1. 仓库管理
- 添加 SVN 仓库（URL + 用户名 + 密码）
- 仓库列表持久化存储
- 删除仓库（从本地管理列表移除，不动 SVN 端数据）

### 2. 同步核心
- 监控指定本地文件夹（FileSystemWatcher）
- 检测文件变化事件：Created / Changed / Deleted / Renamed
- 检测到变化后，延迟一小段时间（如 500ms）合并同批次变化，然后自动 commit 到 SVN
- Last-Write-Wins 冲突解决：取本地文件时间戳 vs SVN 最后版本时间戳，以最新的为准强制覆盖
- 同步完成后记录同步事件

### 3. 同步记录查看
- 记录每次同步操作：时间戳、文件路径、操作类型（Add/Modify/Delete/Rename）、结果
- 提供 UI 列表展示同步历史
- 支持滚动加载（分页或无限滚动）

---

## 暂不提供（后续迭代）

- 手动 commit / revert / checkout（已通过首次添加自动完成）
- 冲突手动解决界面
- 多文件夹同时监控
- 文件夹级别的 exclude 规则（.gitignore 类）
- 分块同步 / 增量同步
- 同步记录导出

---

## 数据流

### 流程一：上行同步（本地 → SVN）

```
文件变化（Created/Changed/Deleted/Renamed）
    ↓
FileSystemWatcher 捕获事件
    ↓
收集到变化队列，5秒防抖计时开始
    ↓
5秒内无新变化，防抖计时器触发
    ↓
遍历变化文件，逐个判断：
  ├── 新文件 → svn add
  ├── 已删除 → svn delete
  └── 修改 → 对比本地时间戳 vs SVN 版本时间戳
            └── 若本地更新 → svn commit
    ↓
commit 完成后刷新文件列表视图
    ↓
写入同步记录（operation=Add/Update/Delete）
```

### 流程二：下行同步（SVN → 本地）

```
定时器触发（默认每60秒）
    ↓
获取本地工作副本当前版本号（svn info）
获取服务器 HEAD 版本号（svn info -r HEAD URL）
    ↓
服务器版本 == 本地版本？
    ├── 是 → 无事发生，等待下次轮询
    └── 否 → 执行 svn update
              ↓
         svn update 遇见锁定文件（如 Excel 打开中）？
           ├── 无锁定 → 正常更新，刷新文件列表
           └── 有锁定 → 跳过被占用文件，加入 pendingUpdates 队列
                        继续更新其他文件
                        通知用户：「file.xlsx 被占用，已跳过」
              ↓
         svn update 遇见冲突？
           ├── 无冲突 → 更新成功，刷新文件列表
           └── 有冲突 → 按 Last-Write-Wins 处理：
                        若本地时间戳 > 服务器 → 保留本地，svn resolved
                        若服务器更新 → svn update 合并后本地覆盖，svn resolved
              ↓
写入同步记录（operation=Update/ConflictResolved）
    ↓
下次轮询 → 先重试 pendingUpdates 队列中的文件
           → 再检查新变化
           → 连续失败3次 → 标记「同步失败」，提示用户手动处理
```

### 流程二补充：锁定文件处理（Best-Effort Update）

```
svn update 执行
    ↓
遇到被锁定文件（如 Excel/Word 打开中）？
    ├── 否 → 正常更新，刷新文件列表
    └── 是 → 跳过该文件，加入 pendingUpdates 队列
              ↓
         继续更新其他文件
              ↓
         通知用户：「file.xlsx 被其他程序占用，已跳过，将在下次轮询时重试」
              ↓
下次轮询（60秒后）→ 先重试 pending 文件 → 再检查新变化
    
连续失败 3 次 → 标记为「同步失败」→ 提示用户手动关闭占用程序
```

### 流程三：添加仓库

#### 从网络添加（Checkout）
```
用户点击「从网络添加仓库」
    ↓
弹出 CheckoutWindow，输入：
  - 仓库名称（必填，用户自定义）
  - SVN 仓库 URL（必填，格式验证）
  - 用户名（可选）
  - 密码（可选）
  - 本地路径自动生成：%APPDATA%/SVNFileBox/workcopies/<仓库名称>
    ↓
点击「确认」
    ↓
本地工作副本目录已存在？
    ├── 是 → 提示「该仓库已存在」，终止
    └── 否 → 执行 svn checkout URL --username user --password pass
              ↓
         checkout 成功？
           ├── 失败 → 提示错误，清空残留目录，让用户重试
           └── 成功 → 继续
    ↓
保存仓库到配置列表（name + url + localPath + username + encryptedPassword）
    ↓
启动 FileSystemWatcher 监控该仓库
启动下行轮询定时器（每60秒）
    ↓
加载文件列表视图
```

#### 添加本地仓库
```
用户点击「添加本地仓库」
    ↓
弹出文件夹选择框，选择已有的 SVN 工作副本目录
    ↓
验证选中的文件夹是有效的 SVN 工作副本（存在 .svn）
    ├── 无效 → 提示「请选择有效的 SVN 工作副本」
    └── 有效 → 自动获取仓库名称（文件夹名）和 URL（svn info）
    ↓
保存仓库到配置列表
    ↓
启动 FileSystemWatcher + 下行轮询
加载文件列表视图
```

### 配置文件路径

- 配置目录: `%APPDATA%/SVNFileBox/`
- 配置文件: `%APPDATA%/SVNFileBox/config.json`
- 工作副本目录: `%APPDATA%/SVNFileBox/workcopies/`（从网络添加时自动创建）
- 日志目录: `%APPDATA%/SVNFileBox/logs/`

### 流程四：冲突处理（Last-Write-Wins）

```
svn update 执行时发现冲突文件
    ↓
获取冲突文件列表
    ↓
遍历每个冲突文件：
  获取本地文件最后修改时间
  获取服务器版本最后修改时间（svn log -r HEAD）
    ↓
本地修改时间 >= 服务器修改时间？
    ├── 是 → 保留本地版本，执行 svn resolved 标记冲突解决
    └── 否 → 先 svn update（合并），再以本地为准 svn commit，svn resolved
    ↓
全部冲突文件处理完毕
    ↓
写入同步记录（operation=ConflictResolved）
```

---

## 功能详细设计

### 仓库管理

- **添加仓库**: 输入 SVN URL + 用户名 + 密码（可选），本地路径由用户选择文件夹
- **仓库模型**:
  - `name`: 显示名称
  - `url`: SVN 仓库地址
  - `localPath`: 本地工作副本路径（用户选择的文件夹）
  - `username` / `password`: 认证信息（密码加密存储）
  - `isActive`: 是否启用监控
- **持久化**: JSON 配置文件（`~/.svnfilebox/config.json`），密码用 DPAPI 加密
- **删除仓库**: 从配置中移除，不动 SVN 端数据
- **仓库列表 UI**: 左侧上半部分显示所有已添加仓库，点击切换到对应工作副本
- **按钮区 UI**: 左侧下半部分，垂直排列5个操作按钮：
  - 🌐 从网络添加仓库
  - 📂 添加本地仓库
  - 📋 查看同步记录
  - ⚙️ 设置
  - ℹ️ 关于
- 点击「查看同步记录」时，右侧区域切换为同步记录列表；其余时间右侧显示文件列表

### 首次 Checkout

- 用户添加仓库时，若本地路径为空或不是有效 SVN 工作副本，则自动执行 `svn checkout`
- 命令: `svn checkout --non-interactive URL localPath [--username user [--password pass]]`
- Checkout 完成后自动开始监控
- 若本地已是有效工作副本（已有 `.svn`），跳过 checkout，直接监控

### 双向同步机制

#### 上行：本地 → SVN（FileSystemWatcher + 5s 防抖）
- **FileSystemWatcher** 监控本地文件夹（包含子目录）
- 监听事件: Created / Changed / Deleted / Renamed
- **5秒防抖窗口**: 收集同批次变化，5秒内没有新变化才触发提交
- 每次触发: 比对 SVN 版本时间戳，执行 `svn add` / `svn commit`
- `svn commit` 消息格式: `Auto-sync: [Add|Update|Delete] <文件路径>`
- commit 完成后自动刷新文件列表视图

#### 下行：SVN 服务器 → 本地（轮询机制）
- **定期轮询** SVN 服务器 HEAD 版本号（默认 60 秒间隔，最短可配置为 10 秒）
- 每次轮询获取本地工作副本当前版本号，对比服务器 HEAD
- 若服务器版本 > 本地版本，执行 `svn update` 拉取新变更
- `svn update` 若遇冲突（本地文件也在修改），按 **Last-Write-Wins** 处理：
  - 冲突文件保留本地版本，用 `svn resolved` 标记冲突解决
  - 若冲突未解决，阻止后续本地变更上行，直到冲突处理完毕
  - `svn update` 完成后自动刷新文件列表视图
- 轮询间隔可配置

#### 冲突处理（Last-Write-Wins）
- commit 前比较 **本地文件最后修改时间** vs **SVN 服务器版本最后修改时间**
- 若本地更新，直接 commit 覆盖
- 若 SVN 更新，先 `svn update` 再 commit（保证本地最新）
- 多设备场景: 以最后一次 commit 的时间戳为准，强制同步
- 冲突发生时记录到同步记录，类型标记为 `ConflictResolved`

### 设置页面

| 配置项 | 类型 | 默认值 | 可修改 |
|--------|------|--------|--------|
| 启用自动同步 | 布尔（是/否） | 是 | **否**（固定为是） |
| 同步周期（下行轮询间隔） | 整数（分钟） | 1 分钟 | 是（范围：1~30 分钟） |
| 冲突决策 | 枚举 | Last-Write-Wins | **否**（暂不可修改） |
| 代理设置 | 字符串（URL） | 空（无代理） | 是 |
| 同步记录保留天数 | 整数（天） | 30 天 | 是（范围：7~365 天） |
| 开机启动 | 布尔（是/否） | 否 | 是 |
| 最小化到托盘 | 布尔（是/否） | 是 | 是 |
| 语言 | 枚举（中文/English） | 跟随系统 | 是 |

**说明**：
- 自动同步固定启用，不提供关闭入口，确保 DropBox 体验始终生效
- 同步周期控制服务器下行轮询频率（每 N 分钟检查一次服务器 HEAD 版本）
- 冲突策略 MVP 阶段固定为 Last-Write-Wins，不开放修改
- 代理用于 SVN 命令行（`http_proxy` / `https_proxy` 环境变量）
- 同步记录保留天数到期后自动清理旧记录，防止文件膨胀
- 开机启动通过注册表（Windows）或 LaunchAgent（macOS）实现
- 关闭窗口时默认最小化到系统托盘，双击托盘图标恢复窗口

---

## UI 设计（继承 SVNFileManager）

### 主窗口布局

```
┌─────────────────────────────────────────────────────────┐
│  [S] SVNFileBox                              [工具栏按钮] │
├────────────┬────────────────────────────────────────────┤
│ Repository │ Path: /path/to/workspace          [Refresh]│
│ ─────────  ├────────────────────────────────────────────┤
│ 📁 repo1   │ ← 文件列表 或 同步记录列表（按需切换）         │
│ 📁 repo2   │                                            │
│ ─────────  │                                            │
│ 按钮区:    │                                            │
│ + 从网络添加│                                            │
│ + 添加本地  │                                            │
│ + 查看同步记录│                                          │
│ + 设置      │                                            │
│ + 关于      │                                            │
├────────────┴────────────────────────────────────────────┤
│ Status bar: 当前路径 + 操作状态                           │
└─────────────────────────────────────────────────────────┘
```

### 左侧边栏（220px）

**上半部分**: 仓库列表（点击切换）

**下半部分**: 按钮区（垂直排列）
- 🌐 从网络添加仓库（Checkout）
- 📂 添加本地仓库
- 📋 查看同步记录
- ⚙️ 设置
- ℹ️ 关于

点击「查看同步记录」后，右侧区域切换为同步记录列表视图（不再显示文件列表）。

### 文件列表列

| 列 | 内容 | 可排序 |
|----|------|--------|
| Type | **文件夹图标**（黄色）或**文件图标按扩展名显示**（蓝色系，通过 Drawing 绘制图标并冻结缓存，支持 .cs/.xaml/.png/.jpg/.pdf 等常见扩展名，继承 SVNFileManager1 的 FolderIconConverter 实现方式） | 否 |
| Name | 文件名，「返回上级目录」为首行（仓库根目录则不显示） | 是（默认升序） |
| Status | 彩色徽章：蓝=Modified，绿=Added，红=Deleted，橙=Conflicted，灰=Unversioned，紫=Missing | 是（默认降序） |
| Size | 文件大小（文件夹留空），自动换算 B/KB/MB/GB | 是（默认降序） |
| Modified | 最后修改时间，格式 `yyyy-MM-dd HH:mm` | 是（默认降序） |

**排序规则**: 点击列头切换升序/降序；文件夹始终排在文件前面。

### 文件列表交互（参考 SVNFileManager1）

#### 文件浏览导航

- **双击文件夹** → 进入该子目录，更新文件列表视图
- **上级目录行** `..`:
  - 列表顶部显示一行，文字显示为「返回上级目录」
  - 点击该行 → 返回父目录
  - **仓库根目录下不显示**该行（无上级可返回）
- 当前路径显示在文件列表顶部 Path 栏

#### 支持的操作

| 操作 | 触发方式 | 说明 |
|------|----------|------|
| 在文件资源管理器中打开 | 右键菜单「打开文件夹」 | 打开当前文件所在目录 |
| 复制路径 | 右键菜单「复制路径」 | 复制文件完整路径到剪贴板 |
| 粘贴文件 | 右键菜单「粘贴」或 `Ctrl+V` | 从剪贴板粘贴文件到当前目录 |
| 新建文件夹 | 右键菜单「新建文件夹」 | 弹出输入框，创建新文件夹 |
| 重命名 | 右键菜单「重命名」 | 弹出输入框，重命名文件/文件夹 |
| 删除 | 右键菜单「删除」 | 确认后删除，显示对话框 |
| 拖拽文件进入 | 拖拽外部文件到列表 | 复制文件到当前目录（冲突时提示） |
| 双击打开 | 双击文件/文件夹 | 文件用系统默认程序打开，文件夹进入目录 |
| 刷新 | 工具栏 Refresh 按钮 或 右键「刷新」 | 重新加载当前目录文件列表 |

#### 右键菜单项

```
在资源管理器中打开
复制路径
─────────────
粘贴
新建文件夹
重命名
─────────────
删除
─────────────
刷新
```

#### 拖拽处理

- `DragEnter` / `DragOver`: 仅允许 `FileDrop` 格式，显示 Copy 光标
- `Drop`: 遍历拖拽的文件列表，复制到当前目录
  - **文件**: 复制到当前目录，若目标已存在则弹出确认框询问是否覆盖
  - **文件夹**: 递归复制整个目录树到目标路径，若目标已存在则提示跳过（不覆盖）
  - 复制完成后自动刷新文件列表视图

#### 粘贴处理（Ctrl+V）

- 检查剪贴板是否有文件列表
- 目标目录为当前工作副本路径
- **文件**: 复制到当前目录，若目标已存在则弹出确认框询问是否覆盖
- **文件夹**: 递归复制整个目录树到目标路径，若目标已存在则提示跳过（不覆盖）
- 复制完成后自动刷新文件列表视图
- **与拖拽处理逻辑保持一致**

#### 样式参考 SVNFileManager

- **配色**: 主色 `#1E88E5`（蓝），状态徽章色同 SVNFileManager（SvnStatusToColorConverter）
- **图标**: FolderIconConverter / FileIconConverter，System.Drawing 绘制，冻结后缓存
- **工具栏按钮**: 文本 + emoji，参考 SVNFileManager 顶部按钮样式
- **左侧边栏**: 220px，白色背景，Repository 列表，底部操作按钮
- **状态栏**: 底部蓝底白字，显示当前路径 + 操作状态

---

## 技术栈

- **主程序**: WPF (.NET 8) + C#（Windows 原生 UI）
- **后端同步引擎**: 纯 C# + SVN 命令行（`svn` CLI）
- **数据持久化**: SQLite（仓库列表 + 同步记录）
- **配置存储**: JSON 配置文件（`%APPDATA%/SVNFileBox/config.json`）

---

## 项目索引

| 项目 | 技术栈 | 说明 |
|------|--------|------|
| SVNFileBox | WPF (.NET 8) | 主程序，仓库管理 + 双向同步 |
| SVNFileManager1 | WPF (.NET 8) | 文件管理器（ SVNFileBox 的功能原型） |
| SVNFileBoxWpf | ? | （待确认） |

**注意**: SVNFileBox 是 WPF 项目，代码在 `~/aiworks/projects/repos/SVNFileBox/src/` 目录下。

---

## 验收标准

1. 添加仓库后，指定本地文件夹能自动监控并同步到 SVN
2. 文件新建/修改/删除，能自动 commit 到 SVN，5秒防抖后触发
3. 服务器端有更新时，60秒内自动检测到并执行 svn update 拉取本地
4. svn update 遇冲突时，按 Last-Write-Wins 处理并记录
5. 同步记录正确显示每次操作的轨迹（包括冲突解决）
6. 多个设备同时修改同一文件，最终以最后写入时间为准统一
7. 主窗口显示文件列表，列样式与 SVNFileManager 一致（Type/Name/Status/Size/Modified）